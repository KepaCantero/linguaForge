sonar.projectKey=KepaCantero_linguaForge
sonar.organization=kepacantero

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=linguaForge
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# ============================================================
# FALSE POSITIVES - CONFIGURATION
# ============================================================

# Exclude test files from analysis
sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/__tests__/**,**/*.spec.ts,**/*.spec.tsx

# Ignore TypeScript errors in build artifacts
sonar.tsconfig.excludePaths=**/node_modules/**,**/.next/**,**/out/**,**/dist/**

# ============================================================
# THREE.JS / REACT-THREE-FIBER PROPERTIES
# ============================================================
# These are valid properties for @react-three/fiber but SonarQube doesn't recognize them
# We use eslint-disable comments in code for these cases

# ============================================================
# STYLED COMPONENTS / EMOTION PROPERTIES
# ============================================================
# jsx, global are valid properties for styled-components/emotion

# ============================================================
# WINDOW.GLOBALTHIS
# ============================================================
# For Next.js SSR, we use globalThis instead of window
